<h2><%= current_user.name %></h2>
<p>
  <% if current_user.avatar_url.nil? %>
    <%= image_tag('profile.png', alt: '', width: 150, height: 150) %>
  <% else %>
    <%= image_tag(current_user.avatar_url(:standard)) %>
  <% end %>
</p>